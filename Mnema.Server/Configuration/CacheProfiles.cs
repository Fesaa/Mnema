namespace Mnema.Server.Configuration;

public static class CacheProfiles
{
    public const string FiveMinutes = nameof(FiveMinutes);
    public const string OneHour = nameof(OneHour);
    public const string OneDay = nameof(OneDay);
    public const string OneWeek = nameof(OneWeek);
}